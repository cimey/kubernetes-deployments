apiVersion: v1
kind: Service
metadata:
  name: products-cluster-service
  namespace: default
  labels:
    app: products
spec:
  selector:
    app: products
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      name: http
  type: ClusterIP
